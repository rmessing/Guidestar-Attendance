<% provide(:title, 'Update Parent') %>
<% provide(:button_text, 'Save Changes') %>
<h1>Update Parent Profile</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
	<%= render 'form' %>
	<%= button_to "Delete Adult", parent_path(@parent.id), method: :delete %>
  </div>
</div>
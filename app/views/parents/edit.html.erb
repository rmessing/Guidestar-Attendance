<% provide(:title, 'Update Parent') %>
<% provide(:button_text, 'Save Changes') %>

<h1>Update Parent Profile</h1>

<div class="row">
  <div class="col-md-4 col-md-offset-4">
	<%= render 'form' %>
	<%= button_to "Delete Adult", parent_path(@parent.id), method: :delete, data: {confirm: "Are you sure?"}  %>
  </div>
</div>


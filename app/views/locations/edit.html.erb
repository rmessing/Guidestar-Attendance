<% provide(:title, 'Update Location') %>
<% provide(:button_text, 'Save Changes') %>
<h1>Update Location Profile</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
	<%= render 'form' %>
	<%= button_to "Delete Location", location_path(@location.id), method: :delete %>
  </div>
</div>
<% provide(:title, "Child Profile") %>

<div class="container">
	<div class="profile">
		<h1>Child Profile</h1>
		<span>NAME:</span>  <%= @child.fname %> <%= @child.mname %> <%= @child.lname %><br><br>

		<% if @child.group_id %>
			<% group = Group.find(@child.group_id) %>
           	<span>CLASS:</span> <%= group.name %>
        <% end %><br><br>

        <% if @child.group_id != nil && group.location_id != nil %>  	
           	<% location = Location.find(group.location_id) %>
           	<span>LOCATION:</span> <%= location.name %><br><br>
      	<% end %>
		
		<% if current_center.admin? %>
			<% center = Center.find(@child.center_id) %>
		  	<span>CLIENT:</span> <%= center.name %><br><br>
	    <% end %>

	    <% if @child.birth_date %>
	    	<span>BIRTHDAY:</span> <%= @child.birth_date.strftime("%B %d, %Y") %><br><br>
	    <% end %>
		
		<% if @child.parents.length != 0 %>	
			<span>ADULTS:</span>
			<% @child.parents.each do |parent| %>
				<a href="<%= parent_path(parent.id) %>"><span><%= parent.fname %> <%= parent.lname %></span></a>
			<% end %>
		<% end %>
		<br><br><br>

		  <b><%= link_to "REGISTER-NEW-CHILD", controller: "children", action: "new", id: @child.center_id %><b>
	</div>


</div>



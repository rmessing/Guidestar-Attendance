<% provide(:title, 'All Children') %>
<h1>Select Child</h1>

<ul class="objects">
  <% @children.each do |child| %>
    <li>
		<a href="<%= child_path(child.id) %>">
		<h4><%= child.fname %>
			<%= child.mname %>
			<%= child.lname %></h4>
		</a>
    </li>
  <% end %>
</ul>
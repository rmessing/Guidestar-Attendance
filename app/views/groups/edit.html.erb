<% provide(:title, "Update Group") %>
<% provide(:button_text, "Save Changes") %>

<h1>Update Group Profile</h1>

<div class="row">
	 <div class="col-md-4 col-md-offset-4">
	 	<%= render 'form' %>
		<%= button_to "Delete Class", group_path(@group.id), method: :delete, data: {confirm: "Are you sure?"}  %>
	</div>
</div>		
<% provide(:title, 'Update Teacher') %>
<% provide(:button_text, 'Save Changes') %>

<h1>Update Teacher Profile</h1>

<div class="row">
  <div class="col-md-4 col-md-offset-4">
	<%= render 'form' %>
	<%= button_to "Delete Teacher", teacher_path(@teacher.id), method: :delete, data: {confirm: "Are you sure?"}  %>
  </div>
</div>

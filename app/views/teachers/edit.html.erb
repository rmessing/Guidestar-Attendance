<% provide(:title, 'Update Teacher') %>
<% provide(:button_text, 'Save Changes') %>
<h1>Update Teacher Profile</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
<%= render 'form' %>
<%= button_to "Delete Teacher", teacher_path(@teacher.id), method: :delete %>
  </div>
</div>
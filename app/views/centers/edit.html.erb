<% provide(:title, "Update Center") %>
<% provide(:button_text, "Save Changes") %>

<h1>Update<%= " #{@center.name} " %>Profile</h1>
<div class="row">
	<div class="col-md-4 col-md-offset-4">
		<%= render 'form' %>
		<%= button_to "Delete Center", center_path(@center.id), method: :delete, data: {confirm: "Are you sure?"} %>
	</div>
</div>